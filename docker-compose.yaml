version: '3.8'

services:
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
      - "8222:8222"
    command: [
      "--js",
      "--sd", "/data",
      "--http_port", "8222"
    ]
    volumes:
      - nats-data:/data

  schema-registry:
    build: .
    ports: